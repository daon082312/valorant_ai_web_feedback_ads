# Vision model slot

Future trained local computer-vision models can be placed here.

Recommended first model classes:

- enemy
- enemy_head
- crosshair
- killfeed_self
- death_banner
- round_end
- spike

The v1 app runs without a model. Training-frame collection is already enabled so a later version can add ONNX inference without changing the UI workflow.
